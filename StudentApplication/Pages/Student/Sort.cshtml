@page
@model StudentApplication.Pages.Student.SortModel
@{
}

<h2>Students</h2>

    <form method="get">
        @* <div style="display:inline-block; margin-right:40px; background-color:red;">
            <label for="sortOrder">Sort by Order:</label>
            <select id="sortOrder" name="sortOrder" asp-for="SortOrder" asp-items="@Model.SortOrderOptions">
                <option value=""> Select Sorting Order </option>
            </select>
        </div>
         <div style="display:inline-block; margin-right:40px;background-color:red;">
            <label for="sortBy">Sort by Type</label>
            <select id="sortBy" name="sortBy" asp-for="SortBy" asp-items="@Model.SortByOptions">
                <option value="">Select Sorting property</option>
            </select>
        </div>  *@


        <div style="display:inline-block; margin-right:40px">
            <label for="sortOrder">Sort by Order:</label>
            <select id="sortOrder" name="sortOrder">
                <option value="">Select Sorting Order</option>
                <option value="asc">Ascending</option>
                <option value="desc">Descending</option>
            </select>  
        </div>
        <div style="display:inline-block; margin-right:40px;">
            <label for="sortBy">Sort by Property:</label>
            <select id="sortBy" name="sortBy">
                <option value="">Select Sorting Property</option>
                <option value="Id">ID</option>
                <option value="FirstName">FirstName</option>
            </select>
        </div>


        <div style="display:inline-block;">
            <button type="submit">Sort</button>
        </div>
    </form>

<div class="mt-6">
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>FirstName</th>
                <th>LastName</th>
                <th>Email</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var student in Model.students)
            {
                <tr>
                    <td>@student.Id</td>
                    <td>@student.FirstName</td>
                    <td>@student.LastName</td>
                    <td>@student.Email</td>

                </tr>
            }
        </tbody>
    </table>
</div>
